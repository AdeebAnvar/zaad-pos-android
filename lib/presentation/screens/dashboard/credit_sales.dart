import 'package:flutter/material.dart';

class CreditSalesHistory extends StatelessWidget {
  const CreditSalesHistory({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
