import 'package:flutter/material.dart';

class PaybackScreen extends StatelessWidget {
  const PaybackScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
